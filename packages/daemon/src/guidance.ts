/**
 * 决策 27：长任务异步化引导（appendSystemPrompt）。
 *
 * 群里踩过「处理失败：turn timeout」—— agent 把耗时任务（构建/取数/批处理）放在
 * 同一轮里同步等待，10 分钟上限一到整轮被掐。与其把超时调大（只是推迟死线），
 * 不如每次 turn 都追加一条执行方式引导：长任务一律后台化、进度写文件、本轮先
 * 短汇报。这样一轮的耗时通常能压在分钟级，超时只是兜底。
 *
 * 措辞保持中性（决策 21）：只说执行方式，不出现「飞书」「群」「机器人」——
 * 那些信号会让 agent 以为自己在群里、该主动回消息。
 * 可用 INSTEAD_AGENT_GUIDANCE 环境变量整体替换。
 */
export const DEFAULT_TURN_GUIDANCE = [
  '长任务执行规则：',
  '- 预计耗时超过 2~3 分钟的操作（构建、测试、下载、取数、批处理等），不要在本轮同步等待完成；',
  '- 用平台的后台执行能力启动，把进度和结果写入文件，本轮先简短汇报「已后台启动 + 如何查进度」；',
  '- 用户追问进度时读文件汇报，不要重新跑一遍。',
].join('\n');
